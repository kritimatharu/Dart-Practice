// WAP in Dart that perform some math

void main() {
  var a = 300;
  var b = 200;
  var sum = a + b;
  var diff = a - b;
  var div = a / b;
  var mul = a * b;
  var mod = a % b;

  print( "sum is ${sum}\ndiff is ${diff}\ndiv is ${div}\nmul is ${mul}\nmod is ${mod}");
}
